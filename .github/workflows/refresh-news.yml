name: Refresh news CSV

on:
  schedule:
    # 07:00 UTC daily (~midnight PT). Change the cron below to shift the time.
    - cron: "0 7 * * *"
  workflow_dispatch: {}

permissions:
  contents: write

jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install feedparser pandas

      - name: Fetch latest news
        run: python scripts/fetch_news.py

      - name: Commit and push if changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          if ! git diff --quiet -- data/news.csv; then
            git add data/news.csv
            git commit -m "Auto-refresh news.csv $(date -u +%Y-%m-%d)"
            git push
          else
            echo "No changes in news.csv, skipping commit."
          fi
